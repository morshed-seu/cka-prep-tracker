# captured 2026-07-30T03:26:16Z on sandbox
# --- snippet ---
# 
# set -e
# cd ~/i6/bc
# echo "=== where did each tool put the image? ==="
# for ns in default buildkit; do
#   echo "--- containerd namespace: $ns"
#   sudo ctr -n $ns images ls name~=i6/bc 2>/dev/null | awk "NR==1||/i6\/bc/{print \"    \" \$1, \$3}"
# done
# echo
# echo "=== same bytes? compare the two manifest digests ==="
# sudo ctr -n default  images ls name==docker.io/i6/bc:nerdctl  | awk "NR==2{print \"  nerdctl  \" \$3}"
# sudo ctr -n buildkit images ls name==docker.io/i6/bc:buildctl | awk "NR==2{print \"  buildctl \" \$3}"
# echo
# echo "=== exporter type=local: the FILESYSTEM, no image at all ==="
# rm -rf out; sudo buildctl build --frontend dockerfile.v0 --local context=. --local dockerfile=. \
#   --output type=local,dest=out 2>&1 | tail -2
# ls out | head; echo "  content of out/who:"; cat out/who
# echo
# echo "=== exporter type=oci: an OCI layout tarball, no runtime involved ==="
# sudo buildctl build --frontend dockerfile.v0 --local context=. --local dockerfile=. \
#   --output type=oci,dest=img.tar 2>&1 | tail -2
# tar tf img.tar
# 
# --- output ---
=== where did each tool put the image? ===
--- containerd namespace: default
    REF DIGEST
    docker.io/i6/bc:nerdctl sha256:05f3b7aaf28e1d4aa690c36f6cd226aae12ed6e37d84b8a137f55312cda0590f
--- containerd namespace: buildkit
    REF DIGEST
    docker.io/i6/bc:buildctl sha256:05f3b7aaf28e1d4aa690c36f6cd226aae12ed6e37d84b8a137f55312cda0590f

=== same bytes? compare the two manifest digests ===
  nerdctl  sha256:05f3b7aaf28e1d4aa690c36f6cd226aae12ed6e37d84b8a137f55312cda0590f
  buildctl sha256:05f3b7aaf28e1d4aa690c36f6cd226aae12ed6e37d84b8a137f55312cda0590f

=== exporter type=local: the FILESYSTEM, no image at all ===
#6 copying files 7.86MB 0.1s done
#6 DONE 0.1s
ls: cannot open directory 'out': Permission denied
  content of out/who:
cat: out/who: Permission denied
