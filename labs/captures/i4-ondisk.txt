# captured 2026-07-28T10:33:34Z on sandbox
# --- snippet ---
# 
# echo "=== the SAME deletion, as overlayfs stores it on disk ==="
# sudo bash -c '
# for d in /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/*/fs; do
#   n=$(basename $(dirname $d))
#   motd=$(stat -c "%F" "$d/etc/motd" 2>/dev/null || echo "absent")
#   opq=$(getfattr -n trusted.overlay.opaque --only-values "$d/etc/apk" 2>/dev/null || echo "-")
#   printf "snapshot %-3s etc/motd=%-20s etc/apk opaque=%s\n" "$n" "$motd" "$opq"
# done'
# echo
# echo "=== the container upper dir in detail ==="
# sudo bash -c 'ls -l /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/*/fs/etc/motd 2>/dev/null'
# 
# --- output ---
=== the SAME deletion, as overlayfs stores it on disk ===
snapshot 1   etc/motd=regular file         etc/apk opaque=-
snapshot 10  etc/motd=character special file etc/apk opaque=-
snapshot 5   etc/motd=regular file         etc/apk opaque=-
snapshot 8   etc/motd=character special file etc/apk opaque=y

=== the container upper dir in detail ===
-rw-r--r-- 1 root root  284 Mar 25  2025 /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/1/fs/etc/motd
c--------- 1 root root 0, 0 Jul 28 16:31 /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/10/fs/etc/motd
-rw-r--r-- 1 root root  284 Jan  4  2025 /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/5/fs/etc/motd
c--------- 2 root root 0, 0 Jul 28 16:31 /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/8/fs/etc/motd
