# captured 2026-07-28T10:32:33Z on sandbox
# --- snippet ---
# 
# sudo apt-get install -y attr >/dev/null 2>&1
# UP=$(sudo find /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots -maxdepth 5 -path "*/fs/etc/apk" -type d 2>/dev/null | head -1)
# echo "container upper dir: $UP"
# echo "=== overlayfs marked the replaced directory OPAQUE ==="
# sudo getfattr -d -m - "$UP" 2>&1
# echo "=== and the deleted file is a 0:0 character device (exactly what B5 showed) ==="
# sudo ls -l "$(dirname "$UP")/motd" 2>&1
# echo "=== so the on-disk representation and the tar representation are different things ==="
# 
# --- output ---
container upper dir: /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/5/fs/etc/apk
=== overlayfs marked the replaced directory OPAQUE ===
=== and the deleted file is a 0:0 character device (exactly what B5 showed) ===
-rw-r--r-- 1 root root 284 Jan  4  2025 /var/lib/containerd/io.containerd.snapshotter.v1.overlayfs/snapshots/5/fs/etc/motd
=== so the on-disk representation and the tar representation are different things ===
