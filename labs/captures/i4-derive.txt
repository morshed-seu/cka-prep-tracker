# captured 2026-07-28T10:31:18Z on sandbox
# --- snippet ---
# 
# cd ~/i4
# sudo nerdctl rm -f box1 2>/dev/null || true
# sudo nerdctl rmi alpine-derived:1 2>/dev/null || true
# echo "=== run a container from the image we just walked ==="
# sudo nerdctl run -d --name box1 alpine:3.21 sleep 600
# echo "=== delete a file, replace a whole directory, add a file ==="
# sudo nerdctl exec box1 sh -c "ls -l /etc/motd; rm /etc/motd"
# sudo nerdctl exec box1 sh -c "rm -rf /etc/apk && mkdir /etc/apk && echo replaced > /etc/apk/marker"
# sudo nerdctl exec box1 sh -c "echo hello > /root/added.txt"
# echo "=== commit it: the changes become ONE new layer ==="
# sudo nerdctl commit box1 alpine-derived:1
# sudo nerdctl images | grep -E "derived|REPOSITORY"
# 
# --- output ---
=== run a container from the image we just walked ===
5bd747a1ddf251dff278436f2a55cf5510de403c4c6d241a20388350dcd1decf
=== delete a file, replace a whole directory, add a file ===
-rw-r--r--    1 root     root           284 Jan  4  2025 /etc/motd
=== commit it: the changes become ONE new layer ===
sha256:46f0b63bfdfd6112c976a22b4e5ffb1ecfbbdfa59f3bb70836c07efdfc94d957
REPOSITORY        TAG     IMAGE ID        CREATED                   PLATFORM         SIZE       BLOB SIZE
alpine-derived    1       b17409a50d7c    Less than a second ago    linux/amd64      8.528MB    3.649MB
