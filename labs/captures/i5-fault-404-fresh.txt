# captured 2026-07-28T11:41:53Z on sandbox
# --- snippet ---
# set +e
# echo "### the same pull on a node that does NOT already have the layer"
# sudo ctr namespace rm i5x >/dev/null 2>&1
# sudo ctr -n i5x images pull --plain-http localhost:5000/broken/busybox:1.37 2>&1 | grep -v "|" | tail -4
# echo
# echo "### versus the node that does — the earlier pull left the layer in its content store"
# sudo ctr -n i5 content ls -q 2>/dev/null | grep -c . | xargs echo "blobs in namespace i5:"
# sudo ctr -n i5 content ls -q 2>/dev/null | grep 436a1b1fd078 && echo "  ^ the deleted layer, still local"
# --- output ---
### the same pull on a node that does NOT already have the layer
Completed pull from OCI Registry (localhost:5000/broken/busybox:1.37)	elapsed: 0.1 s	total:  1.0 Ki	(8.7 KiB/s)	
localhost:5000/broken/busybox:1.37      	saved	
application/vnd.oci.image.manifest.v1+json sha256:7a3ebe5bfd1a4a19797d20b0c0bb39d44393e9a03fd852c0865b0f540d868df0
Completed pull from OCI Registry (localhost:5000/broken/busybox:1.37)	elapsed: 0.1 s	total:  2.1 Mi	(17.5 MiB/s)	

### versus the node that does — the earlier pull left the layer in its content store
blobs in namespace i5: 36
sha256:436a1b1fd078ee8e117111472724c2827077657189af7a781829d0825d48d2ab
  ^ the deleted layer, still local
