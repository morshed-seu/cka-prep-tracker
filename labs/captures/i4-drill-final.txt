# captured 2026-07-28T10:59:11Z on sandbox
# --- snippet ---
# 
# cd ~/i4
# sudo rm -rf rootfs-bad rootfs
# sudo python3 oci-inspect.py derived-oci 1 --extract rootfs | tail -3
# sudo test -e rootfs/etc/motd && echo "present (wrong)" || echo "absent - correct"
# 
# --- output ---
      deleted etc/motd

rootfs ready: rootfs
absent - correct
