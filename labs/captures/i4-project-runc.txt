# captured 2026-07-28T10:35:54Z on sandbox
# --- snippet ---
# 
# cd ~/i4
# sudo rm -rf bundle && sudo mkdir -p bundle && sudo mv rootfs bundle/rootfs
# cd bundle
# sudo runc spec
# echo "=== point the I2 bundle at the rootfs we unpacked by hand ==="
# sudo jq ".process.terminal=false | .process.args=[\"/bin/sh\",\"-c\",\"echo I am running from a rootfs I unpacked myself; cat /etc/alpine-release; ls /etc/apk; test -e /etc/motd && echo MOTD PRESENT || echo motd correctly absent\"]" config.json > /tmp/c.json && sudo mv /tmp/c.json config.json
# sudo runc run i4demo
# echo "--- runc exit: $? ---"
# sudo runc delete i4demo 2>/dev/null || true
# 
# --- output ---
=== point the I2 bundle at the rootfs we unpacked by hand ===
I am running from a rootfs I unpacked myself
3.21.7
marker
motd correctly absent
--- runc exit: 0 ---
