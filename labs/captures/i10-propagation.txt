# captured 2026-07-31T02:50:43Z on sandbox
# --- snippet ---
# bash ~/i10-6.sh
# --- output ---
== the host side must itself be a mount point, and shared, or nothing can propagate
/home/ubuntu/i10-prop shared
for comparison, the root filesystem systemd set up:
/      shared

---------- mounted with rprivate ----------
the container's own view of the mount's propagation:
464 454 8:1 /home/ubuntu/i10-prop /prop rw,relatime - ext4 /dev/sda1 rw,discard,errors=remount-ro,commit=30
A. host mounts a tmpfs under the source -> can the container see it?
   no  - the container sees an empty directory where a filesystem is mounted
B. container mounts a tmpfs inside -> can the host see it?
   no  - the host sees an empty directory

---------- mounted with rslave ----------
the container's own view of the mount's propagation:
464 454 8:1 /home/ubuntu/i10-prop /prop rw,relatime master:1 - ext4 /dev/sda1 rw,discard,errors=remount-ro,commit=30
A. host mounts a tmpfs under the source -> can the container see it?
   YES - the mount appeared inside (this is HostToContainer)
f
B. container mounts a tmpfs inside -> can the host see it?
   no  - the host sees an empty directory

---------- mounted with rshared ----------
the container's own view of the mount's propagation:
464 454 8:1 /home/ubuntu/i10-prop /prop rw,relatime shared:246 master:1 - ext4 /dev/sda1 rw,discard,errors=remount-ro,commit=30
A. host mounts a tmpfs under the source -> can the container see it?
   YES - the mount appeared inside (this is HostToContainer)
f
B. container mounts a tmpfs inside -> can the host see it?
   no  - the host sees an empty directory

== cleanup
