# captured 2026-07-28T11:40:35Z on sandbox
# --- snippet ---
# set +e
# echo "### exactly which plugin sections carry config_path in containerd 2.3.3"
# sudo containerd config default | awk "
# /^ *\[plugins/ {sect=\$0}
# /config_path/ {print NR\":  \"sect\"  ->  \"\$0}
# "
# --- output ---
### exactly which plugin sections carry config_path in containerd 2.3.3
31:      [plugins.'io.containerd.cri.v1.images'.registry]  ->        config_path = ''
152:    [plugins.'io.containerd.nri.v1.nri']  ->      plugin_config_path = '/etc/nri/conf.d'
259:    [plugins.'io.containerd.transfer.v1.local']  ->      config_path = ''
