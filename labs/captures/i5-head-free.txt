# captured 2026-07-28T11:35:15Z on sandbox
# --- snippet ---
# set +e
# REPO=library/alpine; REG=https://registry-1.docker.io
# ACC="application/vnd.oci.image.index.v1+json,application/vnd.docker.distribution.manifest.list.v2+json"
# TOKEN=$(curl -s "https://auth.docker.io/token?service=registry.docker.io&scope=repository:$REPO:pull" | jq -r .token)
# rem(){ curl -sI -H "Authorization: Bearer $TOKEN" -H "Accept: $ACC" $REG/v2/$REPO/manifests/3.21 | tr -d "\r" | awk -F": " "tolower(\$1)==\"ratelimit-remaining\"{print \$2}"; }
# echo "start:            $(rem)"
# for i in 1 2 3; do curl -s -I -o /dev/null -H "Authorization: Bearer $TOKEN" -H "Accept: $ACC" $REG/v2/$REPO/manifests/3.21; done
# echo "after 3x HEAD:    $(rem)"
# for i in 1 2 3; do curl -s    -o /dev/null -H "Authorization: Bearer $TOKEN" -H "Accept: $ACC" $REG/v2/$REPO/manifests/3.21; done
# echo "after 3x GET:     $(rem)"
# --- output ---
start:            91;w=3600
after 3x HEAD:    91;w=3600
after 3x GET:     90;w=3600
