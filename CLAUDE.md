# CLAUDE.md — CKA prep site

Dependency-free static site (GitHub Pages) that takes a user from zero to passing the CKA exam. No build step, no external requests — plain HTML/CSS/JS only. It is growing into a **three-track journey**:

| Track | Tracker | Lessons | Plan | Status |
|---|---|---|---|---|
| **Beginner** — the Linux/networking/distributed-systems machinery Kubernetes is built from | `beginner.html` | `materials/b0…b14.html` | [`docs/BEGINNER-TRACK.md`](docs/BEGINNER-TRACK.md) | ✅ complete (B-S1…B-S19; 225 checkpoints) |
| **Intermediate** — composing that machinery into containers, runtimes, images, CNI | `intermediate.html` | `materials/i0…i13.html` | [`docs/INTERMEDIATE-TRACK.md`](docs/INTERMEDIATE-TRACK.md) | 🚧 I-S1…I-S13 done; **tracker complete — all 195 cp tickable (I0–I13, 78 h)**, `PUBLISHED=[0,1,2,3,4,5,6,7,8]` (`materials/i0.html`…`i8.html` shipped; I-S14 writes `materials/i9.html`) |
| **Advanced** — Kubernetes itself, through the CKA | `index.html` | `materials/w0…w8.html` | [`docs/PLAN.md`](docs/PLAN.md) | ✅ complete |

Decisions already made (don't re-ask): fully self-contained lesson depth; all four extras (fault scripts, quizzes, cheat sheets, mock exam); implement one phase per session, in track/week order.

## Commands

**Authoring a module page? Read [`docs/AUTHORING.md`](docs/AUTHORING.md) — the one-page session runbook.** It supersedes re-reading the long tables below.

```bash
python3 -m http.server 8000         # serve locally → http://localhost:8000

# checkers — run all four before considering a phase finished
tools/check-links.sh                # all three tracks: tracker checkpoint <-> lesson anchor, both ways
                                    #   index.html    data-id="wN-M" <-> materials/wN.html  id="cp-N-M"
                                    #   beginner.html data-id="bN-M" <-> materials/bN.html  id="cp-bN-M"
                                    #   intermediate.html data-id="iN-M" <-> materials/iN.html id="cp-iN-M"
tools/check-html.py                 # tag-balance; defaults to every tracker + materials/cheatsheets/mock
tools/check-page.py [--anatomy]     # raw <word> in <pre> (check-html.py CANNOT see this), &-as-entity,
                                    #   broken links, dangling deep anchors, lesson.js wiring,
                                    #   un-captured output. Tuned to zero false positives: if it
                                    #   fires it is real. --anatomy adds advisory block-structure checks.
node --check assets/lesson.js       # syntax-check the shared JS

# authoring helpers
tools/scaffold-module.py i4         # generate materials/i4.html from the tracker section:
                                    #   every stub, id and boilerplate, with each checkpoint's text
                                    #   and "Under the hood" note inlined as comments. Write prose only.
tools/publish-module.sh i4          # PUBLISHED bump + sibling Site lists + previous pager next + verify.
                                    #   Pre-flights the page and refuses one containing TODO.
tools/vm.sh run|cap|put|get|clean   # run lab snippets on the `sandbox` VM without the hang/transfer traps
```

There is no build step, package manager, linter, or test framework beyond the `tools/` scripts above — run the four checkers (plus a visual spot-check in the browser: theme toggle, done-sync with the tracker, anchors landing right) before considering a phase finished.

## Architecture

- **Two trackers, same shape**: `index.html` (CKA weeks) and `beginner.html` (B-modules) are each the source of truth for their own checkpoints. Each carries its own inline `PUBLISHED` array (module/week numbers that have a lesson page) and `EXTRAS` array (`{t:'title', href:'...'}`), which drive the `lesson` links and sidebar entries. Publishing a lesson page = adding its number to that tracker's `PUBLISHED`.
- **Anchor scheme**, one rule for both tracks — **anchor id = `cp-` + data-id with a leading `w` stripped**: `index.html`'s `<li data-id="wN-M">` ↔ `materials/wN.html`'s `<article class="lesson" id="cp-N-M">`, and `beginner.html`'s `<li data-id="bN-M">` ↔ `materials/bN.html`'s `id="cp-bN-M"`. Bidirectional, enforced by `tools/check-links.sh`.
- **Shared client state**, all via `localStorage`, same-origin so it works free on GitHub Pages: `cka-prep-v1` (checkpoint done/undone — **one key for both tracks**, ids namespaced by the `b` prefix, so cross-track sync is free and each tracker's reset button filters to its own prefix), `cka-theme` (light/dark, `data-theme` attribute), `cka-collapsed-v1` / `cka-grp-collapsed-v1` (section + group collapse). `assets/lesson.js` wires every lesson page into these; the two trackers have their own copies of the tracker/theme logic inline.
- `assets/site.css` is the single stylesheet for every page (both trackers, lessons, cheat sheets, mock exam) — includes `@media print` rules cheat sheets depend on to fit one page. Beginner-track blocks (`.tracks .objectives .prereq .outcome .lesson.project .lesson.drill .k8s-link .langpair .modlist .godeep`) live at the end; the box headings are CSS `::before` content, so authors write `<div class="k8s-link">` and get the mandatory heading for free.
- The **three-pill track switcher** (`.tracks`) sits in every sidebar directly after `.brand-row`, with `class="on"` on the current track and Intermediate as an inert `<span>`. Root pages link `beginner.html`/`index.html`; pages in `materials/`, `cheatsheets/`, `mock/` prefix `../`. Per-page "Site" lists are *not* merged across tracks — the switcher is the only crossing point.
- Lesson pages are otherwise independent static files with no shared templating — conventions below are enforced by convention + the check scripts, not by a build step.

## Phase roadmap (update this table at the end of each phase)

| Phase | Scope | Status |
|---|---|---|
| P0 | Scaffolding: site.css, lesson.js, 📖 links, check scripts | ✅ done (`5877d46`) |
| P1 | materials/w0.html + w1.html | ✅ done (`02c3276`) |
| P2 | materials/w2.html (kubeadm/PKI/lifecycle) + cheatsheets/etcd-card.html | ✅ done |
| P3 | materials/w3.html (workloads/scheduling/Helm/Kustomize) | ✅ done |
| P4 | materials/w4.html (networking; heaviest diagrams) | ✅ done |
| P5 | materials/w5.html (storage/CSI) | ✅ done |
| P6 | materials/w6.html + labs/faults/*.sh + cheatsheets/triage-template.html | ✅ done |
| P7 | materials/w7.html (RBAC/auth/speed) + cheatsheets/commands.html | ✅ done |
| P8 | materials/w8.html + mock/ (exam-1, solutions, setup script) + cheatsheets/exam-day.html + final QA + artifact banner update | ✅ done |
| EP0 | ~~Beginner-mode infra: toggleable `.foundation`/`.analogy` panels~~ | ⛔ superseded by R0 |
| EP1 | ~~Enrich W0 + W1 with toggleable `.foundation` callouts (pilot)~~ | ⛔ superseded by R1 |
| R0 | Retire `#basics` toggle infra (lesson.js/site.css/every page header); rewrite `materials/foundations.html`; new conventions below | ✅ done |
| R1 | Rewrite materials/w0.html + w1.html: plain-English-first voice, full depth kept | ✅ done |
| R2 | Rewrite materials/w2.html (kubeadm/PKI/lifecycle) | ✅ done |
| R3 | Rewrite materials/w3.html (workloads/scheduling/Helm/Kustomize) | ✅ done |
| R4 | Rewrite materials/w4.html (networking; heaviest diagrams) | ✅ done |
| R5 | Rewrite materials/w5.html (storage/CSI) | ✅ done |
| R6 | Rewrite materials/w6.html (troubleshooting) | ✅ done |
| R7 | Rewrite materials/w7.html (RBAC/auth/speed) | ✅ done |
| R8 | Rewrite materials/w8.html + final cross-link/voice-consistency QA over all 9 files | ✅ done |

P0–P8 complete; the site is a full self-contained internals-first curriculum. EP0–EP8 (the toggleable beginner-enrichment pass) is **superseded** — feedback was that bolting optional panels onto otherwise jargon-dense prose wasn't enough; the plan pivoted to rewriting every lesson's core prose in plain English with full depth kept, one unified voice, nothing to toggle (R0–R8, see "Plain-English lesson voice" below and `docs/PLAN.md`'s addendum for the pivot history). R0–R8 are done per the table above (R1 dissolved every `.foundation` callout box in w0/w1 into the main "The concept" prose — EP1's pilot only ever touched w0/w1, so w2–w8 never had any `.foundation`/`.analogy` boxes to begin with) — phases haven't always landed strictly in order when sessions ran in parallel, so check each week's own status above rather than assuming, and check `git log --oneline` for what's actually committed vs. just marked done on disk. Note: `shellcheck` was unavailable in the P8 environment — `mock/setup-exam-1.sh` passed `bash -n` and mirrors the (shellchecked) `labs/faults/` patterns, but run shellcheck on it when available.

## Beginner-track roadmap (update this table at the end of each session)

Full spec: [`docs/BEGINNER-TRACK.md`](docs/BEGINNER-TRACK.md) — 15 modules (B0–B14), **225 checkpoints**, ~74 hours, self-paced. (225, not the spec's original ~200: each module's mini project and debugging drill are their own tickable checkpoint in a trailing "Project & drill" group, because the module anatomy makes each its own lesson article and `check-links.sh` demands one tracker checkpoint per anchor. So a module's total = its spec group counts + 2; B0 has no drill, +1; B14's groups already are its capstone/gauntlet, +0. `docs/BEGINNER-TRACK.md`'s per-module tables carry the corrected totals.) **Every phase below is sized to one session, start to finish**: it ends with green checkers, a commit, and a site that renders with nothing half-written. No session depends on another session's in-memory context.

| Phase | Scope | Status |
|---|---|---|
| B-S1 | `docs/BEGINNER-TRACK.md` — curriculum spec (docs-only commit) | ✅ done |
| B-S2 | Wiring, no content: `site.css` additions; generalize `check-links.sh`/`check-html.py`; track switcher into every sidebar; `index.html` hero + prefix-scoped reset; `foundations.html` banner + go-deep links; stub `beginner.html` (`PUBLISHED=[]`) | ✅ done |
| B-S3 | `beginner.html` sections B0–B7 (119 checkpoints) | ✅ done |
| B-S4 | `beginner.html` sections B8–B14 (106 checkpoints) + hours panel | ✅ done |
| B-S5 | `materials/b0.html` — **pattern-setter** (lesson anatomy, `k8s-link`, `langpair`, project/outcome blocks); review before continuing | ✅ done |
| B-S6 | `materials/b1.html` — shell, files, filesystem tree (16 cp) | ✅ done |
| B-S7 | `materials/b2.html` — processes, threads, signals (18 cp) | ✅ done |
| B-S8 | `materials/b3.html` — kernel space, user space, system calls (14 cp) | ✅ done |
| B-S9 | `materials/b4.html` — users, groups, permissions, capabilities (15 cp) | ✅ done |
| B-S10 | `materials/b5.html` — files, VFS, storage, overlayfs (18 cp) | ✅ done |
| B-S11 | `materials/b6.html` — namespaces, chroot, pivot_root (15 cp) | ✅ done |
| B-S12 | `materials/b7.html` — cgroups v2: CPU/memory/pids/io limits, throttling, OOM (14 cp) | ✅ done |
| B-S13 | `materials/b8.html` — Networking I: layers/ARP/CIDR, routing, TCP/UDP, tcpdump, the 5-rung ladder (18 cp) | ✅ done |
| B-S14 | `materials/b9.html` — Networking II: netns/veth/bridge, ip_forward+NAT+conntrack, netfilter hooks/tables/filter-vs-nat/iptables-nft/rule-order, DNAT+statistic LB + NodePort + unix sockets, CNI-v0.1 project + drill (20 cp) | ✅ done |
| B-S15 | `materials/b10.html` — Names: DNS &amp; resolution: resolver library/nsswitch, /etc/hosts, resolv.conf + ndots, search-list fan-out, recursive-vs-authoritative, `dig +trace` delegation, systemd-resolved stub, record types, `dig` anatomy, TTL, negative caching, dnsmasq-zone-into-a-namespace project + drill (13 cp) | ✅ done |
| B-S16 | `materials/b11.html` — Service management: init/PID 1, unit types, writing a unit, Restart/Type, After-vs-Requires, systemctl verbs, drop-ins, EnvironmentFile, exit-code-vs-signal, journalctl, journal persistence, targets/blame/cgls, supervisor-to-unit project + drill (14 cp) | ✅ done |
| B-S17 | `materials/b12.html` — Security fundamentals: threat modelling, least privilege/defense in depth, authn-vs-authz, hashing-vs-encoding-vs-encryption, symmetric/asymmetric, signatures, SSH keys + agent, run a CA, CSR, SANs, reading certs + chain verification, TLS then mutual TLS, secret leakage via argv/environ, seccomp + AppArmor, mTLS project + four-fault drill (16 cp) | ✅ done |
| B-S18 | `materials/b13.html` — Distributed systems: why distribute, partial failure, no shared clock, the eight fallacies + latency ladder, stateless-vs-stateful, leader/follower replication, strong-vs-eventual + CAP, quorum arithmetic, Raft + split brain + fencing, idempotency, backoff + jitter, the reconciliation loop + watch-vs-poll, leases/heartbeats, reconciler + quorum project, two-fault drill (15 cp) | ✅ done |
| B-S19 | `materials/b14.html` (minibox capstone, gauntlet, assessment — 10 cp) + `labs/beginner/*.sh` (8 faults + runner) + `mock/beginner-final.html` &amp; `-solutions.html` + cross-track QA | ✅ done — **beginner track complete** |

**The beginner track is complete** as of B-S19 (2026-07-25): 15 module pages (`materials/b0…b14.html`), 225 checkpoints, ~74 hours, plus `labs/beginner/` (8 gauntlet fault scripts + a runner) and `mock/beginner-final.html` / `beginner-final-solutions.html` (30 recall questions + 8 practical tasks, 100 points, 66 to pass). Cross-track QA at the end of B-S19 confirmed: 225 beginner + 171 advanced checkpoints, the track switcher on every page, zero broken links or dangling anchors site-wide, and every module page's pager `next` now pointing at its sibling (b14's leaves the track for `index.html`). The **Intermediate track** now has a spec — see the roadmap below.

**Resume recipe for any beginner-track session:** read this table → read the target module's section in `docs/BEGINNER-TRACK.md` → `git log --oneline -8` (what actually landed, vs. just marked done) → run the three checkers for a green baseline → do the work → checkers → commit → tick the row here and update the `cka-materials-plan` memory.

Beginner specifics that differ from the CKA workflow below: anchors are `data-id="bN-M"` ↔ `id="cp-bN-M"` (one rule covers both tracks: **anchor = `cp-` + data-id with a leading `w` stripped**); labs run on a single throwaway Multipass VM named `sandbox` (Ubuntu 24.04), never on the `cp`/`node01`/`node02` cluster; every conceptual lesson ends with a mandatory **"Where this shows up in Kubernetes"** (`.k8s-link`) block deep-linking the advanced lesson anchor; **Docs** links point at `man` pages and kernel docs, not kubernetes.io.

## Intermediate-track roadmap (update this table at the end of each session)

Full spec: [`docs/INTERMEDIATE-TRACK.md`](docs/INTERMEDIATE-TRACK.md) — 14 modules (I0–I13), **195 checkpoints**, ~78 hours, self-paced. Same one-session-per-phase sizing as the beginner track. Thesis: *the container ecosystem added almost no mechanism — it added contracts*, so every module runs **you already built this → here is the spec → here is the real implementation → now break it**.

| Phase | Scope | Status |
|---|---|---|
| I-S1 | `docs/INTERMEDIATE-TRACK.md` — curriculum spec (docs-only commit) | ✅ done |
| I-S2 | Wiring, no content: `.specref` in `site.css`; third track in `check-links.sh`; switcher pill made live in all 35 existing pages; stub `intermediate.html` (`PUBLISHED=[]`); `labs/intermediate/seed/` | ✅ done |
| I-S3 | `intermediate.html` sections I0–I6 (96 checkpoints) | ✅ done |
| I-S4 | `intermediate.html` sections I7–I13 (99 checkpoints) + hours panel | ✅ done — **tracker complete: 195 cp / 78 h** |
| I-S5 | `materials/i0.html` — **pattern-setter** (`.specref`, back-reference habit, toolchain table); review before continuing | ✅ done — `PUBLISHED=[0]`; all output captured from a live run |
| I-S6 | `materials/i1.html` — the bridge module (14 cp): user namespaces, PSI, cpuset, the eBPF device controller, seccomp by hand, TUN/VXLAN | ✅ done — `PUBLISHED=[0,1]`; all output captured live on `sandbox` |
| I-S7 | `materials/i2.html` — the OCI runtime spec (16 cp) | ✅ done — `PUBLISHED=[0,1,2]`; all output captured live on `sandbox` |
| I-S8 | `materials/i3.html` — runc internals (14 cp) | ✅ done — `PUBLISHED=[0,1,2,3]`; all output captured live on `sandbox` |
| I-S9 | `materials/i4.html` — the OCI image spec (16 cp) | ✅ done — `PUBLISHED=[0,1,2,3,4]`; all output captured live on `sandbox`; ships `labs/intermediate/oci-inspect.py` |
| I-S10 | `materials/i5.html` — the OCI distribution spec (15 cp) | ✅ done — `PUBLISHED=[0,1,2,3,4,5]`; all output captured live on `sandbox` against Docker Hub and a local `registry:2`; ships `labs/intermediate/pull.py` and `limiter.py` |
| I-S11 | `materials/i6.html` — building images (13 cp) | ✅ done — `PUBLISHED=[0,1,2,3,4,5,6]`; all output captured live on `sandbox`, which gained **BuildKit v0.32.0** + QEMU (no second VM, no Docker); ships `labs/intermediate/tinybuild.py` |
| I-S12 | `materials/i7.html` — containerd, shims, snapshotters (18 cp) | ✅ done — `PUBLISHED=[0,1,2,3,4,5,6,7]`; all output captured live on `sandbox`; ships `labs/intermediate/ctrpy.py` (does `ctr run`'s whole job from Python) |
| I-S13 | `materials/i8.html` — CRI: the interface the kubelet speaks (16 cp) | ✅ done — `PUBLISHED=[0,1,2,3,4,5,6,7,8]`; all output captured live on `sandbox`; ships `labs/intermediate/toycri.py` + `toybundle.py` (a CRI server real `crictl` drives end to end) |
| I-S14 … I-S18 | `materials/i9.html` … `materials/i13.html`, one module per session | |
| I-S19 | `labs/intermediate/i13-*.sh` + `mock/intermediate-final*.html` + cross-track QA | |

**Resume recipe:** **[`docs/AUTHORING.md`](docs/AUTHORING.md) is the runbook — follow it, not this paragraph.** In short: read this table → read only the target module's section in `docs/INTERMEDIATE-TRACK.md` → `git log --oneline -5` → four checkers for a green baseline → `tools/scaffold-module.py iN` → capture every lab live with `tools/vm.sh cap` → write prose one checkpoint group per commit → `tools/publish-module.sh iN` → tick the row here and update the `intermediate-track-plan` memory.

Intermediate specifics that differ from both other tracks: anchors are `data-id="iN-M"` ↔ `id="cp-iN-M"` (the existing one rule covers it); labs run on a **second** throwaway Multipass VM named `buildbox` (Ubuntu 24.04, 4 vCPU / 8 GB / 40 GB) that **needs internet** for registry pulls, never on `sandbox` or the CKA cluster — **in practice I1 and I2 were both authored on `sandbox`**, which has the full intermediate toolchain installed (containerd 2.3.3, runc 1.5.1, nerdctl, the `~/.venv-intermediate` venv) and working internet — I1 needs no container tooling at all, and I2 needs only `runc`, `crun` and one image pull, so neither justified a second VM; from I4 on, image builds and a local registry make the 4 vCPU / 40 GB `buildbox` worth standing up; every conceptual lesson carries **both** a `.specref` ("What the spec actually says") and the inherited `.k8s-link`, plus an inline back-reference to the beginner lesson that built the primitive by hand; **Docs** links point at the OCI/CNI/CRI specs and the containerd/runc repos. Language policy: **CLI-first (`ctr`/`crictl`/`nerdctl`/`runc`/`skopeo`/`cnitool`/`curl`/`jq`), Python 3 where the subject genuinely is a program** — the ecosystem's Go is linked and explained, never required; Python labs assume the `~/.venv-intermediate` venv from I0 because 24.04 enforces PEP 668. The analogy registry gains an intermediate extension (runtime spec = the written work order, runc = the contractor, shim = the on-site foreman, containerd = the building-services company, image = a flat-pack kit, registry = the parts warehouse, sandbox = the room rented before any guest checks in) — it lives in `docs/INTERMEDIATE-TRACK.md` § "Analogy registry — intermediate extension" and extends, never competes with, the two tables below.

**Verified 2026-07-27 and binding on I-S2+ (re-verify each session):** CKA still targets **k8s v1.35**; **containerd 2.3.3** (2026-07-10) current (noble ships 1.7.28 — the track installs 2.x from the official tarball); **runc v1.5.1**; **CNI plugins v1.9.1**; **crictl v1.36.0**; **nerdctl 2.3.5**; **OCI image-spec v1.1.1 / runtime-spec v1.3.0 / distribution-spec v1.1.1**; **CNI spec v1.1.0** — its own text says *"CNI defines 5 operations: ADD, DEL, CHECK, GC, and VERSION"* and defines **STATUS** alongside them, so quote it that way rather than claiming "six verbs"; **CSI spec v1.11.0**; **in-place pod resize is GA in v1.35** (beta in 1.33), so `UpdateContainerResources` is its GA mechanism. Also confirmed: the advanced track has **no** `securityContext` / Pod Security Standards anchor anywhere — don't invent one, say so and link what exists (I11.5 does). **Confirmed live on 2.3.3 in I-S5:** `containerd config default` emits **`version = 4`** (2.0 moved 2→3, 2.3 moved 3→4) and still carries a legacy `plugins.'io.containerd.grpc.v1.cri'` section alongside the split `io.containerd.cri.v1.runtime` / `io.containerd.cri.v1.images` plugins — I7/I8 must not repeat the bare "version 3" claim. `runc --version` reports `spec: 1.3.0`, matching `ociVersion` in a `runc spec` bundle. **Confirmed live on kernel 6.8.0 in I-S6** (all binding on I11 and I3 in particular): Ubuntu 24.04's `kernel.apparmor_restrict_unprivileged_userns=1` **does not block user-namespace creation** — the stock `unprivileged_userns` profile carries `allow userns` *and* `audit deny capability`, so `unshare --user` succeeds with `CapEff: 0`, and every downstream `EPERM` traces to that empty set with no error message naming AppArmor; the correct workaround is a named profile granting `userns,` for one binary by path, not the sysctl. The containerd/Docker default seccomp profile has **moved to `github.com/moby/profiles`** (all `moby/moby` links 404) and lists **442** syscall names, not "~300", with a `clone` rule masking `0x7E020000` — i.e. *all* `CLONE_NEW*` bits. **Unprivileged overlayfs inside a userns+mountns works**, so `fuse-overlayfs` is not required (that advice predates kernel 5.11). Cgroup delegation gives a user `cpu memory pids` only (no `cpuset`, no `io`), and a process **cannot migrate itself** into a cgroup it created under `user@UID.service` — migration is checked on the common ancestor — so use `systemd-run --user --scope`. The kernel now **refuses** to set an encapsulating interface's MTU above its lower device's (`EINVAL`), so the naive VXLAN-MTU bug is unreachable and must be reproduced by shrinking the underlay afterwards. **Confirmed live in I-S7** (binding on I3 especially): `runc spec` emits **six** top-level keys and **no** `hooks` or `annotations`; deleting the `/proc` mount makes runc **fail to create the container** (libpathrs panics — *"at least one candidate /proc/thread-self path should work"* — then `procReady not received`) rather than merely breaking `ps`, and mounting any non-procfs at `/proc` is refused by an explicit **proc-safety check** — so don't repeat the folklore that a container without `/proc` merely loses `ps`. `cpu.shares` is rescaled **logarithmically** to `cpu.weight` (2→1, 512→59, 1024→100, 4096→303, 262144→10000), and OCI `memory.swap` is memory **plus** swap, so equal `limit`/`swap` yields `memory.swap.max = 0`. **crun 1.14.1** (noble's apt version) reports `spec: 1.0.0` and **refuses `ociVersion` ≥ 1.2.0** with *"unknown version specified"* — at 1.1.0 the same bundle under runc and crun differs only in two namespace inode numbers. A numeric `process.user.uid` absent from the rootfs **starts successfully** (failing later with permission-denied); the real `CreateContainerError` needs a user *name*, one layer up — `ctr run --user appuser` gives `ctr: no users found`. Finally, runc **warns and continues** on unknown capability names (`ignoring unknown or unavailable capabilities`) where the spec says it MUST error — a real conformance gap that silently produces an empty capability set. **Confirmed live in I-S8** (binding on I7 especially, which compares shims): runc uses **three** `nsexec` stages, not two — stage 0 re-execs, stage 1 unshares all namespaces in one call, stage 2 is PID 1 — because `unshare(CLONE_NEWPID)` does not move the caller into the new namespace, so the extra `clone()` is unavoidable; `runc --debug` narrates all of it and is far more readable than `strace`. The re-exec target is **`/proc/self/fd/N`**, a sealed clone of runc's own binary made by `libcontainer/exeseal` (overlayfs preferred, then `memfd_create`, then `O_TMPFILE`) — the CVE-2019-5736 fix. The live `_LIBCONTAINER_*` handshake is `INITPIPE`/`SYNCPIPE`/`LOGPIPE`/`LOGLEVEL`/`FIFOFD`/`INITTYPE`; **`_LIBCONTAINER_STATEDIR` and `_LIBCONTAINER_CONSOLE` do not exist in 1.5.1** (they are runc 1.0 material, and this repo's I3 spec text guessed at them). `strace -f runc create` **never returns**, because `create` deliberately leaves `runc init` alive — always wrap it in `timeout`. `runc state` reports **`pid: 0`** for a stopped container, so the naive "run detached then query the PID" loses **20/20** against a fast container rather than intermittently, and wins 1-for-1 against `sleep` — which is why testing hides it. `runc exec` re-enters **all eight** namespaces and the cgroup (the tracker's "which does it not" has no answer for runc); what it does not inherit is the container's `process` block. `runc update` writes to the **kernel only** — neither the bundle nor `state.json` records it, so a recreate silently reverts. `state.json`'s `config` key is **libcontainer's internal struct**, not the OCI config (`readonlyfs`, `mask_paths`), and `init_process_start` is byte-identical to field 22 of `/proc/<pid>/stat` — the anti-PID-reuse guard. `rm -rf /run/runc/<id>` **appears to work and leaks the cgroup**; `runc delete` removes it. Rootless state lives at `$XDG_RUNTIME_DIR/runc`. **`criu` has no installation candidate on noble**, so `runc checkpoint` fails with an executable-not-found error; the kubelet checkpoint API is **beta and enabled by default since v1.30, still beta**. A `SIGKILL`ed supervisor leaves the container **running** and re-parented to PID 1 (a dead subreaper stops collecting), so `runc create` says "already exists" while plain `runc delete` refuses with "not stopped: running" — a different fault from a dead container with a surviving record, and the distinction is what decides the repair. **Confirmed live in I-S9** (binding on I5, I6 and I7): **image-spec v1.1.1 (2025-03-03) is still current** — v1.1.0 was 2025-02-15, there is no 1.2. A **replaced directory does not reliably yield `.wh..wh..opq`**: committing through containerd emits one `.wh.<child>` per former child, because the default **`walking` differ** compares filesystem *trees* and never reads the overlay upper dir — even though `trusted.overlay.opaque=y` **is** set there (verified with `getfattr`; the `erofs` differ is present but `skip`). Both encodings are legal, so I7 must say a consumer handles either and must not infer the producer from the encoding. A modern Docker Hub index carries **attestation manifests** beside images — `alpine:3.21` has **16 entries for 8 architectures**, the extra 8 marked `platform: unknown/unknown` plus `vnd.docker.reference.type: attestation-manifest` pointing back at the manifest they describe — so any platform selector must skip them twice over (I5's `pull.py` especially). `architecture` and `variant` are **separate fields** (`arm64` + `v8`, never "arm64v8"). OCI vs Docker v2s2 manifests differ in exactly **three media-type strings plus the annotations block**; config and layer digests are byte-identical and the same blobs back both, so converting changes the manifest digest while moving no bytes — and **drops annotations**, since v2s2 has nowhere to put them. The chain-ID recursion `sha256("<prev> <diff_id>")` — single space, both operands keeping their `sha256:` prefixes — **reproduces containerd's snapshot key exactly**; chain IDs are *not* in the OCI spec, they are a containerd construction on top of its ordering guarantee. Recompressing an identical tar at `gzip -1` changes the `digest`, leaves the `diff_id` untouched, and produces a **larger** file. `history − empty_layer = layers` holds, and `jq` reports an absent `empty_layer` as `null`. Overriding an image's entrypoint **discards its `Cmd`** unless args are also given — the run prints an empty line and exits 0, which is precisely a pod spec with `command:` and no `args:`, and the spec is silent on it. Finally, **resolving a tag is not selecting a platform**: a layout from `skopeo copy` without `--all` holds one platform reached by tag with no index, so `--platform` is silently a lie unless the resolved config's own `architecture` is checked — nothing in the real pull path does that check, which is why the arm64/amd64 incident surfaces hours later as `exec format error` rather than as a pull failure. Tooling note: `nerdctl` rejects a one-character container name, `nerdctl commit -c` accepts only `CMD` and `ENTRYPOINT`, and `commit` sweeps the runtime-injected `/etc/resolv.conf` into the layer. **Confirmed live in I-S10** (binding on I6, I7, I8 and I11): **distribution-spec v1.1.1 (2025-01-29) is still current**. Registries differ sharply on `Accept`, and both behaviours are conformant because the spec says SHOULD — **Docker Hub ignores it entirely**, returning the OCI index even when asked only for single-platform types, while **`registry:2` enforces it strictly**, answering `404 MANIFEST_UNKNOWN` for an OCI-stored image requested as Docker v2s2 *and* for a request with no `Accept` at all; asked for a type it does not hold it may instead **down-convert to Docker schema 1** (`application/vnd.docker.distribution.manifest.v1+prettyjws`) and return `200` with a **different `Docker-Content-Digest`** for the same tag — a silent identity change, not an error. So never claim "the server default is a schema-2 manifest" and never generalise negotiation behaviour from one registry. **Measured, not assumed:** three `HEAD`s of a manifest did **not** move Docker Hub's rate-limit counter while three identical `GET`s moved it by one, so re-resolving is free (this is why `imagePullPolicy: Always` is cheap) and the counter tracks something coarser than HTTP requests. Docker Hub sends **both** `ratelimit-*` and `x-ratelimit-*` headers plus `docker-ratelimit-source`, which equals the machine's own public IP; its anonymous token declares `pull_limit_interval 21600` while the same response's headers say `w=3600`, so the two **disagree** and only `ratelimit-remaining` is worth reading. Anonymous tokens expire in **300 s**. A blob `GET` answers **`307`** to `production.cloudfront.docker.com` with a signed, expiring URL — a pull touches **three** hosts (registry, auth service, CDN), and the blob endpoint given a *tag* returns a plain-text `404 page not found` rather than the spec's `{"errors":[…]}` envelope, i.e. it never reached a registry handler. On **push**, skopeo HEADs the blob in *other repositories* before uploading, and cross-repo mount is real: `ubuntu:24.04` to a second repo went **5.552 s → 0.092 s**, with `201` for the 30 MB layer's `?mount=&from=` and a plain `202` for the 2 KB config. `registry:2` **disables deletion by default** (`skopeo delete` → `unsupported`); once enabled, the tag 404s immediately but `du` is unchanged (28.5M → 28.5M) until `garbage-collect` runs (→ 96K). On **containerd 2.3.3** the registry `config_path` lives in **`[plugins.'io.containerd.cri.v1.images'.registry]`** — the path the kubelet's pulls take — with a *second*, non-interchangeable `config_path` under `[plugins.'io.containerd.transfer.v1.local']` used by `ctr`/`nerdctl`; the 1.7-era `io.containerd.grpc.v1.cri` no longer applies, and `ctr --hosts-dir` therefore proves nothing about pods. Mirror requests carry **`?ns=docker.io`**. Finally, two drill corrections: bad credentials return `401` **with** a `WWW-Authenticate` header too — a **`Basic`** challenge from the *auth service* (body `{"details":"incorrect username or password"}`) versus a **`Bearer`** challenge from the *registry* (body `{"code":"UNAUTHORIZED"}`) — so "challenge vs no challenge" is **not** the discriminator; and a manifest whose blob is missing is **invisible on a node that already holds the layer** (containerd's content store is shared across namespaces, so a fresh namespace is not a fresh node), which is why the fault presents as *some* nodes failing and the set changing. **Confirmed live in I-S11** (binding on I7, I8, I11 and I13): **BuildKit v0.32.0** (2026-07-29) installed on `sandbox` from the official tarball, with `buildkitd --containerd-worker` and `tonistiigi/binfmt --install arm64` — **no `buildbox` was needed and none exists**; the I-S9 deferral of that question is settled, and there is deliberately no Docker on the machine. **`WORKDIR` is layer-producing, not config-amending** — unconditionally, even pointed at directories that already exist, because the step goes through the filesystem-snapshot path; nine instructions gave four layers. When its diff is empty the slot holds the **empty layer**: 1024 zero bytes, `diff_id sha256:5f70bf18a086…` (the constant in image-spec's own `config.md` example), gzipping to the 32-byte blob `sha256:4f4fb700ef54…`, and two such steps dedupe to one blob. So classify by mechanism, never by instruction name — and note that "empty layer" (a real layer of 1024 zeros) and `empty_layer: true` (no layer at all) are different things. **BuildKit transfers the build context lazily**, so "the whole context is uploaded to the builder" is legacy-Docker folklore: 101 MB on disk including a 100 MB junk file moved **38 B**, and only `COPY . .` widened it to **104.89 MB / 5.679 s** versus **114 B / 0.887 s** with a `.dockerignore`. A Dockerfile **comment does not invalidate the cache** (comments never become LLB vertices) and neither does `touch` with unchanged content (content is hashed, not mtime — this *was* different on older builders); `chmod` does. **`--no-cache` empties `type=cache` mounts** permanently — four accumulated lines → one, next ordinary build sees two, not five — and cache mounts are keyed by **target path** unless given an explicit `id=`, so unrelated projects share one. Secret *and* cache mounts work with 0.32's **builtin** frontend, no `# syntax=` directive. A `--build-arg` appears **twice** in `history` — as `ARG NAME=value` and inlined into the `RUN` entry via the `|1` prefix — while the only added layer is the empty blob, so a scanner that unpacks layer tars finds nothing: the credential is pure metadata. QEMU emulation is **not** a single multiple: **4.1×** on an interpreter-bound loop, **1.6×** on a hashing step, and an emulated `apk add build-base` **did not complete in 520 s** (~20 s native) — the real failure mode is a CI timeout, so don't quote "5–20×". **`nerdctl build` and `buildctl` produce identical manifest digests but write to different containerd namespaces** (`default` vs `buildkit`), so `nerdctl run` on a `buildctl`-built image decides it is remote and **pulls**, surfacing a connection error against the I5.13 mirror (`?ns=docker.io`) that names neither namespaces nor the build — the same class of trap as I5.13's two `config_path` settings, and worth repeating in I7. `buildctl`'s `type=oci` exporter writes exactly I4.5's layout (`blobs/sha256/`, `index.json`, `oci-layout`); `type=local` writes a bare filesystem with no config and no layer boundaries. A **multi-stage image's `created_by` strips `--from=`**, so nothing records that a build stage existed — the best-built images have the thinnest audit trail, and `created_by` is unvalidated free-form text besides (one of `python:3.13`'s seven layers reports `# debian.sh --arch 'amd64' out/ 'trixie' '@1783900800'`, not a Dockerfile instruction at all). `registry:2`'s blob-upload **`Location` is absolute and already carries `?_state=`**, so prefixing the registry root gives `curl` exit 3 on a malformed URL and the digest must be appended with `&`; `gzip` needs `-n`/`mtime=0` or the header timestamp changes the layer digest for identical bytes. Numbers worth reusing: multi-stage **261.9 MB → 8.598 MB**; the whiteout bill **108,536,725 B / 3 layers vs 3,646,907 B / 2** with a 77-byte `.wh.` layer; `python:3.13` **236,258,363 of 412,660,988 bytes in one `apt-get install`**. Two `tools/vm.sh` traps learned here and worth avoiding: a snippet may **not** contain a nested quoted heredoc (`python3 - <<'PY'` inside the outer snippet breaks on `(`) — generate files with a shell loop instead; and a `jq` program with `$vars` must go in a file read by `jq -f`, since the VM-side shell expands `$s` first. **Confirmed live in I-S12** (binding on I8, I11, I13, and w1/w6 cross-links): the shim's parent is **PID 1**, not containerd — containerd starts `containerd-shim-runc-v2` detached so it re-parents to systemd immediately, and `pstree -ap` of containerd shows **zero** shim children; the classic `containerd → shim → workload` picture is a lineage of who-started-whom, not a live `ps` ancestry, and that detachment is *why* a daemon restart signals nothing (measured: containerd 2805→56260, workload steady at PID 54899 / 02:36 elapsed). A 2.3.3 task dir (`/run/containerd/io.containerd.runtime.v2.task/<ns>/<id>/`) has **no `address` file** — it moved to `bootstrap.json` (`{version:3, address:"unix:///run/containerd/s/<hash>", protocol:"ttrpc"}`) and the socket is a real path under `/run/containerd/s/`, not an abstract-namespace address; its `log` is a **FIFO** (`prwx------`) whose reader is containerd, so `tail`ing it hangs until `CKA_VM_TIMEOUT`. The shim's working dir **is** the runc bundle (`config.json` ociVersion 1.3.0 + `rootfs`), and `runc --root /run/containerd/runc/<ns> state <id>` reads it with no daemon. `ctr run -d --rm` is refused (`--detach and --rm cannot be specified together`). Containerd **namespaces separate names and metadata, not bytes**: the content store path has no namespace in it, so a cross-namespace pull writes **zero** new blob files (measured 472→472) while the progress bar still reports the image's declared size as "transferred"; `ctr -n <typo>` silently invents the namespace (empty table, exit 0). Deleting a container errors (`cannot delete a non stopped container`) **only while its task is RUNNING** — a STOPPED task is removed together with its container in one `ctr container rm`, and a killed task keeps its old PID in `ctr task ls` (unlike runc, which reports pid 0). **`containerd config default` emits `version = 4`** (the tracker's own I7.8 text says 3 — wrong), and this box has **no `/etc/containerd/config.toml` at all** (only `certs.d`), so `ctr plugins ls` (68 plugins, 18 `io.containerd.grpc.v1`) is the honest source of truth, not the config file; the CRI key is `io.containerd.cri.v1.runtime` and mis-keyed config is silently ignored. **One store, two interfaces that disagree**: a blob can be on disk and hash correctly yet make `ctr content get` say `not found`, because `ls` sees the filesystem and `ctr content ls` sees a per-namespace index (472 files vs 183 rows). Single-layer image: **chain ID == diff_id** exactly (alpine's `rootfs.diff_ids[0]` = the snapshot PARENT, the base case of I4.12). Sizes: content 691M, snapshotter **2.2G** (unpacked ≈ 3× compressed — the snapshotter fills the node, not the image list), metadata `meta.db` 4.4M and the **only** store with no upstream to rebuild from (content re-fetchable, committed snapshots re-derivable; a running container's *active* snapshot is a second thing with no upstream). Writes land in an ordinary host dir: `ctr snapshot mounts` prints B5's exact `mount -t overlay … upperdir=…/snapshots/<n>/fs`, and a file written inside the container is `cat`-able from the host immediately. **The tracker's GC demo does not reproduce**: `ctr content fetch` creates a *referenced image*, so nothing is collectible, and its temporary lease is released before `ctr leases ls` can show it — you must delete the image name to see a collection (then it happens in **<5 s**, blob leaves disk too; the "30 s later" folklore is wrong). `ctr leases create` sets `containerd.io/gc.expire` exactly 24 h out (matches the documented default); the scheduler's config (`deletion_threshold=0, mutation_threshold=100, schedule_delay='0s'`) explains the timing. **Socket = node root, no exploit**: `ctr run --mount type=bind,src=/,dst=/host,options=rbind:ro alpine head -1 /host/etc/shadow` printed the shadow line — the socket is `srw-rw---- root:root` and that mode is the whole access control. A single `true` container emits **eight** events in order (`/snapshot/prepare → /containers/create → /tasks/create → /tasks/start → /tasks/exit → /tasks/delete → /containers/delete → /snapshot/remove`); the exit event for an exit-0 container carries **no `exit_status`** field (protobuf omits zero). `nerdctl --name web` does **not** key the containerd container as `web` — it generates a 64-hex ID and stores `web` in a `nerdctl/name` label. **Biggest correction — killing a shim on 2.x does NOT leave an unreapable orphan** (the tracker's premise and I3.14's folklore are 1.x-era): a 200 ms poll shows the workload reparented to PID 1 for one tick (`state Ss ppid 1`) then **GONE by +400 ms**, because containerd monitors the shim's ttrpc socket and logs `shim disconnected` / `cleaning up dead shim`, reaping the orphan within ~2 s — what actually leaks is the **container record** (`ctr container rm` clears it). A dead *runc* supervisor leaves a running container; a dead *shim* does not, because nothing monitors a bare runc the way containerd monitors the shim. Ships `labs/intermediate/ctrpy.py` (does `ctr run`'s whole job from Python, shelling out to `ctr` for the gRPC surface and naming each Go client call it stands in for; ran end-to-end streaming a container's output and exiting 0). Op note: the I5 `registry:2` task was found STOPPED with a stale `sleep` holding `:5000` — `fuser -k 5000/tcp` + `ctr -n default task start -d <id>` revived it for the mini project's pull. **Confirmed live in I-S13** (binding on I9, I11, I12 and w1/w6 cross-links): the default **sandbox image on containerd 2.3.3 is `registry.k8s.io/pause:3.10.2`**, declared at `plugins.'io.containerd.cri.v1.images'.pinned_images.sandbox` — *not* the 1.7-era `sandbox_image` key — and `crictl pull pause:3.10` does **not** satisfy it (`crictl images -o json` reports `pinned:true` only for the configured tag). CRI is **three** plugins on 2.x: `io.containerd.cri.v1.images`, `io.containerd.cri.v1.runtime` (platform `linux/amd64`, while images is `-`), and `io.containerd.grpc.v1.cri` which registers both on the **same socket `ctr` uses**. `crictl`'s default timeout is **10 s and client-side**: a cold node's first `runp` fails with `DeadlineExceeded / stream terminated by RST_STREAM with error code: CANCEL`, which names nothing — only containerd's journal says `failed to pull image "registry.k8s.io/pause:3.10.2"`. The pause process is `/pause`, **1 thread, VmRSS 624 kB, kernel stack blocked in `__do_sys_pause`**; but it does **not** hold the pod's network namespace — that is bind-mount pinned at `/var/run/netns/cni-<uuid>` *before* pause exists (CNI ADD runs first) and the sandbox's own config **joins it by path**, while IPC/UTS really are `/proc/<pause-pid>/ns/…`. The famous pod semantics are one array: the app container's generated OCI config carries `{"type":"network","path":"/proc/<pause>/ns/net"}` plus pid/ipc/uts, with `mount` and `cgroup` pathless. **`shareProcessNamespace` is set on the CONTAINER config, not the pod config** — container `namespace_options.pid=1` (CONTAINER) yields `{"type":"pid"}` and `ps` showing `1 sleep`; `pid=0` (POD) yields a path and `1 pause`; the same field on the *sandbox* config does nothing (though `network:2` there *does* move the sandbox to the host netns, so it parses). `dns_config` becomes a `resolv.conf` the **runtime** writes under `/var/lib/containerd/io.containerd.grpc.v1.cri/sandboxes/<id>/` and bind-mounts over the image's, with `hosts` and `hostname` — all three per-sandbox. **CRI does not deduplicate host ports**: two sandboxes both took `host_port 18080`, both got CNI-HOSTPORT-DNAT rules, the first matched and pointed at a pod with nothing listening (`curl` exit 7) until the first sandbox was deleted — hostPort dedup is a *scheduler* predicate. **A deleted CNI config restored with `mv` does NOT recover the node**: containerd's journal shows exactly one `fs change event(RENAME)` for the move *out*; the move *in* and a `touch` produce none, and only a content-writing `cp` reloads it (`lastCNILoadStatus` OK, NetworkReady true) — and a mid-copy `WRITE` was caught reading a half-written file (`unexpected end of JSON input`). **`UpdateContainerResources` shrinking memory below current usage returns success and the container is OOMKilled (exit 137) two seconds later** — the v1.33+ contract binds the *runtime* not to restart, not the kernel not to kill; `--cpu-share 512` becomes `cpu.weight 59` (the same log rescale as I-S7), and the bundle's `config.json` is **not** rewritten while `crictl inspect` **does** report the new values (unlike bare `runc update` in I3). **`crictl exec` defaults to the STREAMING path and `-s` selects `ExecSync`** — proven by watching TCP: the default opens an ESTAB connection to containerd's plain-HTTP streaming server on `127.0.0.1:<port>` (a real HTTP 404 with headers), `-s` opens none; `crictl port-forward`'s error names the sandbox netns path directly. **Exec output never reaches the container log** (fd 1/2 of the *init* process only), and `maxContainerLogLineSize` is **16384** — a 20,000-byte line arrived as `P`(16384) + `F`(3616) and `crictl logs` reassembled it. `/var/log/containers/` does **not** exist here: the symlink farm is the **kubelet's**, not the runtime's. CRI reserves `<container>_<pod>_<ns>_<uid>_<attempt>`, so recreating a container needs `attempt+1` — the same number as `0.log`/`1.log` and `kubectl`'s RESTARTS. `crictl ps`'s POD column comes **entirely** from `io.kubernetes.pod.*` labels the kubelet sets; a hand-driven container shows `{}` and `unknown`. `runtimeHandlers[0]` has **no `name`** — spec-mandated (`"An empty string denotes the default handler"`), not a bug. Cgroup driver, switched live and reverted: `SystemdCgroup=false` gives `cgroupsPath = /<parent>/<id>`, `true` gives `kubepods-besteffort.slice:cri-containerd:<id>` plus a real `cri-containerd-<id>.scope` unit — and a mismatched node has **both trees**, with `/sys/fs/cgroup/kubepods.slice` present while `/sys/fs/cgroup/kubepods` is absent, and **no error anywhere**. Finally, `dial unix <path>: connect: no such file or directory` is **byte-identical** for a wrong endpoint and a stopped containerd (systemd removes the socket with the unit) — `systemctl is-active containerd` is the discriminator; a socket that exists but speaks something else fails at the **gRPC handshake** (`code = Unavailable` after a pause) rather than at connect. Ships `labs/intermediate/toycri.py` + `toybundle.py` (a CRI server real `crictl` v1.36.0 drives end to end: runp/create/start, CRI-format logs read back by `crictl logs`, namespace join, `ExecSync`, proto-order teardown). Two traps from building it: `/run` is **noexec** on Ubuntu so a rootfs unpacked there fails `exec: "/bin/sh": permission denied` (which is why containerd splits `/run/containerd` from `/var/lib/containerd`), and `shutil.rmtree` on a bundle whose rootfs is still bind-mounted **deletes the shared source tree** — it happened, and the fix is umount + `os.path.ismount` verify + a read-only bind.

## Phase workflow

1. Read `docs/PLAN.md` (per-week content inventory) and skim the target week's checkpoints in `index.html` (`<section id="wN">`) — every lesson expands one checkpoint, absorbing its "Under the hood" note.
2. **Verify version-sensitive facts via web search while authoring** — never from memory. Known: exam runs **Kubernetes v1.35** (checked 2026-07-10, LF CKA page); re-check each session. Page footers state the authored-against version.
3. Write `materials/wN.html` following the conventions below (copy the page skeleton from `materials/w0.html`).
4. Publish: add N to the `PUBLISHED` array in `index.html` (this injects 📖 links + sidebar entry). Cheat sheets/mock go in the `EXTRAS` array (`{t:'title', href:'...'}`). Also add the new page to the static "Site" list in each `materials/*.html` sidebar.
5. Verify: `tools/check-links.sh` && `tools/check-html.py` && `node --check assets/lesson.js`; serve with `python3 -m http.server` and spot-check pages (theme toggle, done-sync with tracker, anchors land right).
6. One commit per phase; end message with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Update the roadmap table above and the `cka-materials-plan` memory file in the same commit/session.

## Lesson page conventions

- One page per week: `materials/wN.html`. One `<article class="lesson" id="cp-N-M" data-id="wN-M">` per tracker checkpoint `data-id="wN-M"` — ids must match bidirectionally (`tools/check-links.sh` enforces).
- Lesson anatomy, in order (h5 headings): **Why it matters** → **The concept** (internals; absorb + expand the tracker's "Under the hood") → **Lab** (`<ol class="lab">`, commands in `<pre class="cmd">`, abridged output in `<pre class="out">` with load-bearing lines in `<b>`) → **Verify** (`<h5 class="verify">`) → **Gotchas** (`<h5 class="gotcha">`) → **Docs** (`<p class="docs-links">`, framed as "what to search in the exam docs"). Logistics-only checkpoints use a compact variant (Why + Steps + Gotchas).
- Each lesson head has the done-toggle: `<label class="donebox"><input type="checkbox"><span class="box"></span>done</label>` — `assets/lesson.js` syncs it with the tracker via localStorage key `cka-prep-v1` (key = the `data-id`).
- End of page: `<div class="quiz" id="quiz">` with 8–12 `<details class="reveal">` questions, then `<nav class="pager">` (prev/next), then `<footer>` with the version line.
- Diagrams: inline SVG using CSS vars (`var(--ink)`, `var(--line)`, `var(--accent)`, `var(--code-bg)`, `var(--muted)`) so both themes work; pipeline-style flows use `<div class="flow"><span>…</span><em>→</em>…</div>`.
- Escape `<`, `>`, `&` inside all code blocks (`&lt;` etc.) — the checker catches breakage but not silent text loss; grep for suspicious raw `<word>` patterns after authoring.
- Labs run against the week-0 lab: 3 Ubuntu 24.04 Multipass VMs named `cp`, `node01`, `node02`, kubeadm + containerd + Flannel (`10.244.0.0/16`).
- Voice: plain-English-first, full depth always included — "here's the machinery, here's the failure signature," but walked up to gradually rather than dropped in cold. See "Plain-English lesson voice" below for the per-lesson rewrite recipe (R0–R8). Every lab ends in something checkable. All ~160 lessons should read as one system.

## Plain-English lesson voice (R0–R8)

Supersedes the old EP0–EP8 toggle-based enrichment pass (history + pivot rationale in `docs/PLAN.md`'s EP0–EP8 addendum). Feedback: bolting optional `.foundation` panels onto otherwise jargon-dense prose wasn't enough — readers experienced fully-detailed lessons as if they were terse hints, because the surrounding sentences assumed background they didn't have. Goal now: rewrite the core prose itself so plain language leads and full depth follows, in one voice, for every reader — nothing hidden, nothing to toggle.

Apply this recipe to every `<article class="lesson">` when rewriting a week (R1–R8):

- **Why it matters** — open with one plain-English sentence naming the thing and why a newcomer would care, before the existing exam-relevance framing. Every lesson gets this, not just primitive-heavy ones.
- **The concept** — plain language first, precision second: define each dense term inline in plain words on first use in the lesson (CRI, gRPC, veth, DNAT, quorum, SAN, admission, reconcile loop…) rather than leaving clarity to an `<abbr>` tooltip alone. Reach for **one** analogy from the registry below where it actually clarifies — don't stack metaphors for the same idea. Then deliver the same internals detail the lesson always had, unabridged: depth is never cut for accessibility, only unpacked more gradually. Expand any paragraph or bullet list that currently compresses several ideas into one dense clause into the connected reasoning a beginner needs to follow. Diagrams stay; give each one a one-sentence plain caption if it lacks one.
- **Lab** — keep every command and expected output verbatim (no accuracy regressions); add a short plain sentence before each step saying what it does and why, not just what to type, where that's currently missing.
- **Verify / Gotchas / Docs** — same structure, rewritten so the *why* behind each gotcha is spelled out rather than compressed into a subordinate clause.
- Keep `<abbr title="short plain definition">term</abbr>` on first jargon use per lesson — cheap, still a useful hover reference even though it's no longer the primary clarity mechanism.
- `materials/foundations.html` remains the standalone primer for cross-cutting fundamentals (processes/containers, APIs/declarative, YAML, TCP/IP+DNS, PKI/TLS, consensus/quorum, Linux networking primitives, systemd/static pods) — link into its sections for an optional deeper dive rather than re-deriving a primitive from scratch inside a week's lesson.

**Canonical analogy registry** — reuse these exact metaphors everywhere so they don't collide across weeks; add to this table (don't invent a competing metaphor for something already listed):

| Concept | Analogy |
|---|---|
| apiserver | the front desk / reception |
| etcd | the filing cabinet / ledger |
| controllers / reconcile loop | thermostats, or housekeeping re-checking a posted note |
| scheduler | the seating host at a restaurant |
| pod | a shipping container |
| Service | directory assistance / a phone book entry |
| Ingress | the building receptionist routing calls to the right room |
| CNI / cluster network | the road network between buildings |
| RBAC | a keycard system |
| certificate / CA | a notarized ID card / the notary everyone already trusts |
| network namespace + veth + bridge | a hotel room, its phone wire, and the switchboard connecting rooms |
| systemd + static pods | the building superintendent who keeps the boiler running directly, before the tenant office can open |
| quorum / consensus | a small committee that only needs a strict majority to act |
| PV / PVC / StorageClass | a self-storage facility: a PV is a physical unit already built on the lot, a PVC is a reservation ticket for a unit of some size, a StorageClass is a standing order telling the facility "build a new unit to this spec whenever no existing unit matches a ticket" |

**Beginner-track extension.** The existing hotel metaphor is promoted into the beginner track's spine: **one hotel = one Linux machine; the city of buildings = the cluster.** Same rule — reuse these exact metaphors, don't invent competing ones.

| Concept | Analogy |
|---|---|
| kernel / user space | the hotel's engineering staff, who alone touch the wiring; guests must file a work order |
| system call | that work-order form — the only way a guest reaches engineering |
| process | a guest staying in the hotel; the PID is their room number |
| signal | a knock on the door with a standard meaning; SIGKILL is security removing them, no conversation |
| mount namespace | the room's own closet — same building, different contents |
| PID namespace | the room's own guest list: they can't see who else is in the hotel |
| cgroup | the room's metered power and water, with a cap the front desk sets |
| capabilities | a keyring of individually-issued permits; root is the master key |
| overlayfs layers | a stack of transparent sheets — you write on the top one, you read through the whole stack |
| DNS resolver | the hotel operator who looks a name up before dialing |
| conntrack | the switchboard's log of calls currently in progress |

**Intermediate-track extension** lives in [`docs/INTERMEDIATE-TRACK.md`](docs/INTERMEDIATE-TRACK.md) § "Analogy registry — intermediate extension" (runtime spec = the written work order, runc = the contractor who leaves, shim = the foreman who stays, containerd = the building-services company, OCI image = a flat-pack kit, digest = the part number that fingerprints the contents, registry = the parts warehouse, snapshotter = the room-prep crew stacking B5's transparent sheets, pod sandbox = the room rented and wired before any guest checks in, CRI/CNI/CSI = the standard order forms). Same rule across all three tables: extend, never compete.

## Other components (specs in docs/PLAN.md)

- `labs/faults/wN-*.sh` (P6): banner + `read -p` confirm + wrong-cluster guard (check node names/context); plant fault; print mission; **no solution in output** — hints/solutions are 3-stage reveals in w6.html. Must be shellcheck-clean.
- `cheatsheets/*.html` (P2/P6/P7/P8): use `.sheet` body class; must fit one printed page (`@media print` rules exist in site.css; check browser print preview).
- `mock/` (P8): exam-1.html (16 weighted tasks, 120-min JS countdown, no solutions), solutions-1.html (per-task solution + verification + rubric, 66% pass line), setup-exam-1.sh.
- After P8: update the claude.ai tracker artifact once to banner-link the GitHub Pages site (artifact URL in the `cka-exam-prep` memory).
